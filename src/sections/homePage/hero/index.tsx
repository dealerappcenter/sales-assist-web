import { Nav } from '@components/index';

export const Hero = () => {
    return (
        <header className='h-screen px-4 flex flex-col bg-white-soft'>
            <Nav />
            <div className='flex-grow container mx-auto'>

            </div>
        </header>
    )
}
